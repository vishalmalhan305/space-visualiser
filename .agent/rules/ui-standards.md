---
trigger: always_on
---

# UI Quality Rules
- Always verify that all images have `alt` tags (HTML).
- Ensure no CSS overflows occur on 375px width (Mobile).
- Check the console for any JavaScript errors during navigation.